# Volume Buddy

macOS menu bar app that adds volume control for DisplayPort audio outputs that lack hardware volume support (e.g. monitor headphone jacks). Intercepts media keys, shows the native volume OSD, and provides a menu bar slider.
